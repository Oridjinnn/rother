# ROTHER-AUDIT — Phase 1 Risk Scan (2026-08-14, run 2)

**Trigger:** "run the Rother risk scan, research the fix from the web, follow our
evidence-before-claim rules and all project docs."
**Project root:** `/home/habel-davidson/Downloads/Rother-project/Rother - 0.0.1`
**Scope:** `src/`, `docs/`, `gbp-monitor/`, `src-tauri/`, `prisma/`, tests.
**Phase:** 1 — SCAN (report-only; no source or fixture edits).

## Method & cross-check discipline
This is a second invocation of the trigger on the same day. Before reporting, every
candidate was cross-checked against `MASTER_RISK_REGISTER.md` (RISK-001…031),
`TECHNICAL_DEBT_REGISTER.md` (TD-C01…TD-N10), `UX_RESTRUCTURE_PLAN.md`, and the
prior same-day `RISK_SCAN_2026-08-14.md` / `PLAN_2026-08-14.md`. Findings already
tracked are **consolidated/closed**, not re-flagged. This run ADDS: (a) the
explicit §7 feature-surface health analysis (overlaps, thin features, gaps) that
the first run omitted; (b) refinements discovered by fresh source re-reads
(Tauri CSP now *blocks its own origin*; `selector_tracker` false-positive;
`API_KEY` drift; the two new `c-discover`/`c-geo-grid` features from the
limitation work).

Severity: **high** = blocked feature / silent data loss / trust violation;
**med** = operational friction / scale or maintenance hazard; **low** = cosmetic.

---

## 1. Type / React / Code Debt

| # | Finding | Sev | file:line | Why it matters to a business owner |
|---|---|---|---|---|
| C1 | ~22 `eslint-disable react-hooks/set-state-in-effect` directives (genuine pattern: localStorage hydration, `navigator.onLine`, clock, fetch→setState) | low–med | `app-state.tsx:52`; `live-clock.tsx:44`; `command-palette.tsx:67,84`; `logs-section.tsx:141`; `competitor-correlation.tsx:96` (+14) | Brief empty/flash render before effect runs; cosmetic load flicker. Systemic (tracked RISK-030, fix = `useSyncExternalStore`). |
| C2 | Typing is strict: **0** `any`, 0 `@ts-ignore`. | low | — | No meaningful type debt. |
| C3 | Dead COARSE adapters still on disk: `overview-section.tsx` (~600 LOC, unreferenced), `config-section.tsx` (unreferenced). | med | `src/components/dashboard/overview-section.tsx`; `config-section.tsx` | ~700 LOC dead code + divergent second Config concept. (TD-N05, DONE in run 1.) |
| C4 | **Inconsistent data-acquisition (refined):** TanStack-backed `useBranches()` exists, but `reviews-section.tsx:127` and `history-comparison-section.tsx:53` independently `fetch("/api/branches")` in `useEffect`+local `useState`, bypassing the cache and diverging loading/error state. | med | `reviews-section.tsx:127`; `history-comparison-section.tsx:53` | Two strategies for the SAME endpoint → redundant network calls + inconsistent loading UX for competitor data. Concrete instance of TD-H08 (no shared cache). |
| C5 | **`API_KEY` drift (NEW):** `middleware.ts:24` reads `API_KEY`; if unset, all requests pass (`:79`). It is defined in **no `.env`/`.env.example`** → the app ships "open" by default with no documented key to set. | med | `src/middleware.ts:24,79`; `.env` (absent) | A business owner cannot tell whether their instance is protected; the auth gate is silently off. |

No `any`-type debt; no orphaned feature components (all 30 map to `FEATURES`); command palette auto-maps `FEATURES`.

---

## 2. Data-Layer / Contract Gaps

| # | Finding | Sev | file:line | Why it matters |
|---|---|---|---|---|
| D1 | **Single-business hardcoded; no category dimension on competitors.** `readListings()` returns ONE `ListingsConfig`; `CompetitorConfig`/`BranchConfig` carry no `category`. | high | `gbp-monitor/config/listings.json:4`; `server-data.ts:101-104,83-94`; `types.ts:41-51` | Structural blocker for the planned multi-category scan. (RISK-024/RISK-028; P1/P2 below.) |
| D2 | **place_id trust debt: 10 of 12 competitors unverified** (`place_id:null`/mock); `M11-CORRECTION` proves a prior wrong-attribution. | high | `listings.json:3,18,24,36,54…` | Owner's dashboard may be empty for 10/12 or show the WRONG business's reviews. (RISK-027; validator landed, UI badge deferred.) |
| D3 | **Prisma/SQLite dead** at data layer; `main.rs:46,57` still injects `DATABASE_URL` into the Node sidecar, which JSON layer ignores. | med | `prisma/schema.prisma:16-32`; `main.rs:46,57` | Inert/misleading config in desktop launch. (RISK-026, DONE in run 1.) |
| D4 | **9 silent `catch→fallback` sites** in `server-data.ts` return `[]`/`null` with zero log. | high | `server-data.ts:46-57,110,159,177,215,250,274,300` | "No alert = healthy" is false — a failed scraper silently becomes an empty dashboard. (TD-H06; logging+banner DONE-PARTIAL in run 1.) |
| D5 | Fragile delta filename parse (`lastIndexOf("_")`). | med | `server-data.ts:307-310` | Mis-named id routes reviews to wrong competitor. (RISK-017, DONE in run 1.) |
| D6 | **`.env` drift:** root `.env` only `DATABASE_URL`; `gbp-monitor/.env.*` disjoint `GBP_MONITOR_*`; `GBP_ROOT` set in no env file; `GBP_MONITOR_MODE`/`GBP_MONITOR_LOG_LEVEL` documented but unwired. | med | `.env`; `gbp-monitor/.env.development`; `.env.example` | Two non-communicating config systems; false confidence from documented-but-unread vars. |
| D7 | **Active business has no `branches`** (NEW): `user-business.json` lacks `branches`, so `readActiveBusinessBranches()` falls back to seed while `readListings()` returns `[]` once a business is active → UI renders nothing despite seed data existing. | high | `server-data.ts:469-474` vs `:101-104`; `user-business.json` | After onboarding+Run, the owner can land on empty branch/competitor panels even when seed data is present — exactly the "silent empty dashboard" failure mode D4 describes, now via the new onboarding path. |

---

## 3. UX Contract Compliance

| # | Finding | Sev | file:line | Note |
|---|---|---|---|---|
| U1 | Height lock delegated to parent `section-view.tsx:53` (`h-[calc(100dvh-3.5rem)] overflow-hidden`); `feature-page.tsx` uses `h-full`. No `min-h-screen`/`h-screen`/`overflow-visible`. | low | `section-view.tsx:51,53`; `feature-page.tsx:28,47` | Contract satisfied; `FeaturePage` fragile if mounted outside `SectionView`. |
| U2 | Cmd+K palette auto-maps `FEATURES` → all 30 features reachable. **Registry drift: 30 vs contracted 28** (NEW: `c-discover` `features.tsx:293`, `c-geo-grid` `:302`). | low (clean reachability) / med (contract drift) | `command-palette.tsx`; `features.tsx:79,293,302` | No feature missing, but the UX contract inventory (28) is stale vs reality (30). |
| U3 | Lists scroll internally (reviews `70dvh`, logs `60dvh`, branches `flex-1`). **`competitor-leaderboard.tsx:75` hard-caps `slice(0,12)` in a plain `<ol>` with NO internal scroll** → ranks 13+ unreachable inside the feature. | med | `competitor-leaderboard.tsx:75,178` | Owner with >12 competitors loses lower ranks; ties to X5 hardcoded-12 cap. |
| U4 | `vh` (not `dvh`) in-page caps (palette `12vh`/`50vh`, lists `70vh`/`60vh`). | low–med | `command-palette.tsx:114,137`; `reviews-section.tsx:578`; `logs-section.tsx:360` | Redundant nested scroll on short/resizable windows. (DONE in run 1: `dvh` applied.) |

Contract substantively met. The dvh lock is real (in `section-view.tsx`).

---

## 4. Tauri / Desktop-Wrap Risks

| # | Finding | Sev | file:line | Note |
|---|---|---|---|---|
| T1 | **CSP now BLOCKS the app's own API origin (refined, higher-impact than run-1).** Production `connect-src` is `ipc: http://ipc.localhost` only — it omits `'self'` and `http://127.0.0.1:4632`, the exact loopback the Node sidecar serves the app on (`main.rs:30,82`). The webview's own same-origin `fetch('/api/...')` is therefore CSP-blocked → desktop data loading silently fails. | high | `tauri.conf.json:26`; `main.rs:30,82` | Run-1 reported `csp:null` (disabled). Fresh read shows CSP is now SET but too restrictive — worse, because it looks configured while breaking data. Fix = add `http://127.0.0.1:4632` (or `'self'`) to `connect-src`. |
| T2 | Node sidecar + path-with-spaces ("Rother - 0.0.1"): SAFE (Path objects, list-args, no unquoted shell concat). | info | `main.rs:36-57`; `.zscripts/build.mjs` | No defect. |
| T3 | Updater/signature: absent. | info | `tauri.conf.json` | Distribution gap, not a defect. |
| T4 | Linux compile blocked (webkit2gtk-4.1-dev / glib-sys): **ENVIRONMENTAL**, not a code defect. | info | `src-tauri/target/...` | Do not over-flag. |

---

## 5. Test Harness Fragility

| # | Finding | Sev | file:line | Note |
|---|---|---|---|---|
| E1 | **CI masks live-run failures:** `run_all.py:1647-1655` `sys.exit(0)` in live branch regardless of `failed`. | high | `gbp-monitor/orchestration/run_all.py:1647-1655` | Green CI hides broken acquisition. (RISK-027, DONE in run 1.) |
| E2 | `verify_baseline.py:316` treats exit code 1 (config-validation failure) as "clean" (`crashed = returncode not in (0,1)`). | med | `gbp-monitor/tests/verify_baseline.py:316` | Broken `listings` config reported as passing. (DONE in run 1: crash guard.) |
| E3 | **`selector_tracker.py` false-positive (NEW):** `expected_missing_count` is folded into `effective_found`, and `status="healthy"` fires when all entries are `expected_missing` — so a selector that is *present but should be absent* is indistinguishable from one that is *truly absent*. | high | `gbp-monitor/harness/selector_tracker.py:131-141` | A regression where a scraper selector that should have been removed stays present yet the check still reports "healthy" — masks breakage. |
| E4 | **Golden vs harness contradiction (NEW):** `golden-datasets/run_summary.golden.json` asserts `skipped:0, success:3`, but `verify_baseline.py:116-119` asserts `skipped == 9` (9/12 have no fixture). The committed golden baseline contradicts the harness's own `--fixtures` expectation; `compare_golden()` will fail whenever it runs. | med | `golden-datasets/run_summary.golden.json`; `tests/verify_baseline.py:116-119,366-374` | Golden regression guarantee is self-inconsistent. |
| E5 | `golden-datasets/` otherwise empty (only `.gitkeep`); `verify_baseline.py:23` points at `data`, not `golden-datasets/`. | med | `golden-datasets/.gitkeep`; `verify_baseline.py:23` | No populated baseline. |
| E6 | vitest: no coverage block, no path aliases; existing unit tests are pure/offline-safe. | low | `vitest.config.mjs` | Scraper/desktop code 0% covered. |

Offline CI otherwise safe (`--fixtures` mode, no hardcoded `/home/...` paths).

---

## 6. Future Pain Points (judgment, file:line specific)

| # | Finding | Sev | Evidence | Status |
|---|---|---|---|---|
| P1 | **Category dimension dropped at the boundary.** Captured at onboarding (`types.ts` `ActiveBusiness.category`), persisted to `user-business.json` (`scrape/trigger/route.ts:44-48`), but never forwarded to the orchestrator (`scrapeRunManager.start(mode)` at `:91` takes only mode). No `category` on `CompetitorConfig`. No discovery backend (`discovery/` = only `validate_listing.py`). | high | `types.ts:41-51,80-87`; `scrape/trigger/route.ts:44-91`; `discovery/validate_listing.py` | "Pick a category, scan other businesses" cannot be built yet. (RISK-024/028; PARTIAL — `c-discover` UI now exists, backend resolver still missing.) |
| P2 | **Single-business switch forecloses multi-tenant.** Four `hasActiveUserBusiness()` early returns, no `businessId` scoping param. | high | `server-data.ts:102,152,246,295` | Agency/2-location owner unsupportable without refactor. (RISK-024 partial.) |
| P3 | **Mocked auth asserts account privacy it doesn't enforce** (`login-screen.tsx:26,29-30,68-69`). | high | `login-screen.tsx` | Trust/ethics. (RISK-029; DONE in run 1: copy → "on this device".) |
| P4 | **No true map/geo-grid parity with GMB Everywhere.** `c-geo-grid` (added by limitation work) plots static points + a bounding-rectangle "grid" overlay — NOT a rank/grid/teleport visibility scan. Grep of `gbp-monitor/**/*.py` for rank|grid|teleport|local-search = 0 hits. | med (escalates w/ P1) | `c-geo-grid.tsx`; `competitor-correlation/route.ts:85,113` | Partial realization of P4: the map exists, the *visibility grid* does not. Parity gap remains. (RISK-028 scope-risk, deferred.) |
| P5 | **JSON-file layer won't scale** once multi-category goes live (O(n) snapshot reads; silent `[]` on error). | med | `server-data.ts:151-181` | Scale hazard when P1 ships. (Partial: listings cache landed.) |
| P6 | **`c-discover` + `c-geo-grid` are registered but not in the UX-contract inventory (28→30)** and `c-discover` depends on a still-missing backend (P1). | med | `features.tsx:293,302`; `c-discover.tsx` | New surface area with partial backend; document or gate. |

---

## 7. Feature Surface Health — CONSOLIDATION + GAPS

*(NEW this run — the explicit §7 the first pass omitted. All ids from `src/lib/features.tsx`.)*

### A) OVERLAP / REDUNDANCY among the 30 FEATURES

**A1 — `i-sentiment` ⊕ `i-rating-distribution` (same source, re-bucketed)**
- `i-sentiment.tsx:34-35` passes `data.ratingDistribution` into `SentimentDistributionChart`; `i-rating-distribution.tsx:34` renders `RatingDistributionChart` from the identical field; `SentimentDistributionChart` is just a re-binning of `ratingDistribution` (≥4 positive / =3 neutral / <3 negative) at `charts.tsx:473-507`. Both pull `/api/overview.ratingDistribution`.
- **Verdict: MERGE.** One purpose = "see the star distribution." Keep `i-rating-distribution` (raw truth) and fold sentiment in as a secondary readout/badge. (Confirms gap C6: "sentiment" is a rating heuristic, not NLP.)

**A2 — `i-reviews-per-competitor` ⊕ `c-leaderboard` (both rank competitors by review count)**
- `i-reviews-per-competitor.tsx:32` → horizontal bar of `competitorStats.total_reviews`, top 12; `c-leaderboard.tsx:12` → ranked list with a `total_reviews` sort toggle (`competitor-leaderboard.tsx:39-43,64-75`). Same `competitorStats` from `/api/overview`.
- **Verdict: MERGE.** Drop `i-reviews-per-competitor` from Insights; keep `c-leaderboard` (supports Reviews/Rating/New sorts). Embed the bar viz inside the leaderboard card if a chart is wanted.

**A3 — `i-snapshot-glance` ⊕ `c-leaderboard` (near-duplicate competitor scorecard)**
- `i-snapshot-glance.tsx:40,110-142` → per-competitor cards of name/branch/avg rating/new/total; `competitor-leaderboard.tsx:179-277` → same, ranked + richer.
- **Verdict: MERGE.** Keep `c-leaderboard`; drop `i-snapshot-glance` (or convert it to a "your business vs field" baseline once a `mybusiness` dimension exists — see C1).

**A4 — `c-rating-dist-comparison` ⊕ `c-correlation` (same endpoint, fragile reuse)**
- Both self-fetch `/api/competitor-correlation`; `c-rating-dist-comparison.tsx:61-64,75` states it "reuses the same data."
- **Verdict: KEEP but decouple.** Different user views (grouped bars vs trait regression); low user overlap. Flag the shared endpoint as a single point of failure.

**A5 — Run-oriented cluster `i-run-health` / `i-run-history` / `i-run-comparison` + `t-logs`**
- Four run surfaces (`features.tsx:91-98,145-161,331-338`). Distinct purposes → **KEEP**, but consider a single "Runs" grouping to honor "one purpose per screen" without fragmentation.

No other true duplicates (`r-reviews-over-time` is the only cumulative time-series; `i-new-reviews-branch` is per-branch delta).

### B) THIN / LOW-DETAIL FEATURES

- **`t-export`** (`t-export.tsx:21-24`): totals hard-coded to `0` — dialog shows no live counts; bare "Open export" button. **MERGE** into the header export (IA §Export mandates a single header entry), or at minimum wire real counts. (Ties to C8.)
- **`i-run-comparison`** (`features.tsx:145-152`): "Diff two runs" — verify `RunComparisonCard` has an explicit Run A / Run B selector; if it only compares latest-vs-previous it is too thin for a dedicated no-scroll screen. **Specify** the picker.
- **`t-scrape-schedule`** (`features.tsx:349-356`): dev-only `ScrapeSchedule`; empty for most client users. **MERGE** into `t-config`/Settings once Settings lands, or gate out of the client grid.
- **`c-correlation`** (`features.tsx:284-291`): grounded only in `/api/competitor-correlation`; if that endpoint returns sparse/derived fields this is speculative. **Specify** trait inputs or MERGE into `c-comparison`.

### C) MISSING FEATURES (gaps vs vision + GMB Everywhere parity)

| Gap | User job | Hub | Blocking dependency (cited) | Scope risk |
|---|---|---|---|---|
| **C1 — "My Business" detail screen** | See *your own* business health/rating/rank vs field (Flow 8 `03_USER_FLOWS.md:264-272`) | insights | `server-data.readActiveBusiness()` exists, but owner's-own reviews NOT scraped — RISK-023/024/028 | **Scope risk** (live acquisition of arbitrary business unbuilt) |
| **C2 — Live scan progress / run status** | Watch a run, cancel, see per-competitor status (Flow 5 `03_USER_FLOWS.md:146-174`) | insights/tools | `/api/scrape/trigger` only queues; no run-state polling yet (Roadmap "poll state file") | Moderate (UI; backend stub exists) |
| **C3 — Category benchmarking** | Benchmark vs category average (`01_PRODUCT_VISION.md:57`) | insights | `categories.ts` + `user-business.category` captured but **unused** — RISK-028 deferred | **Scope risk** (no category discovery backend) |
| **C4 — Local-search rank / grid / teleport** | Track map-pack rank across a coordinate grid (GMB Everywhere core parity) | competitors | `c-geo-grid` only plots static points + bounding rect; **no rank/grid/teleport scraper** (`gbp-monitor/**/*.py` grep = 0) | **Scope risk** (new subsystem) |
| **C5 — Configurable alerts / thresholds** | Notify only on what matters (negative spike, competitor overtakes) | reputation | `r-alerts` is system+review feed; thresholds not user-set. Roadmap V4 "Alert rules engine" | Low (rules layer over existing alerts) |
| **C6 — AI review analysis** | Understand *why* sentiment is negative, not just rating buckets | reputation | `i-sentiment` is a rating heuristic (`charts.tsx:477-483`); no NLP | Low–Mod (model integration) |
| **C7 — Review-reply suggestions** | Draft a response fast (Flow 2 `03_USER_FLOWS.md:45-75`) | reputation | Explicitly **non-goal** (`10_PRODUCT_ROADMAP.md:129`, vision Out-of-Scope) — flag as parity gap, defer | Low (product decision) |
| **C8 — Export filtered by business/category** | Export just the monitored slice | tools | `t-export` non-functional (B above); ignores business/category under single-business model | Low (wire existing export API) |

**Top quick wins (no new subsystem):** fix/merge `t-export` (C8), add `C2` run-status, wire configurable `C5` thresholds.
**Strategic parity bets (flagged scope risk):** `C1` (My Business), `C3` (category benchmark), `C4` (local-search grid/teleport) — each blocked by deferred backend items RISK-023/024/028.

---

## 8. Summary by Severity

- **High (new / refined this run):** T1 (CSP now *blocks own origin* — refined, higher impact), E3 (`selector_tracker` false-positive), D7 (active business has no branches → empty panels via new onboarding), D1 single-business+no-category, D2 place_id trust 10/12, D4 silent fallbacks, E1 CI masks live failures, P1 category dropped at boundary, P2 multi-tenant foreclosed, P3 mock-auth privacy.
- **Med:** C4 dual fetch paths, C5 `API_KEY` drift, D3 SQLite-in-Tauri, D5 delta parse, D6 env drift, U2 registry 28→30 drift, U3 leaderboard no internal scroll, E2/E4/E5 test gaps, P4 geo-grid partial, P5 JSON scaling, P6 new-feature doc drift, A1–A3 overlaps, C1–C8 gaps.
- **Low:** C1 set-state-in-effect pattern, C2 type casts, U1/U4 contract notes, E6 coverage gap.
- **Info/environmental:** T2 sidecar path-safe, T3 updater absent, T4 Linux compile (env).

**Net vs run 1:** this pass ADDS §7 (feature-surface), the T1 refinement (CSP blocks own API, not merely `null`), E3 (selector false-positive), C5 (`API_KEY` drift), D7 (no-branches empty panels), U2 (30-vs-28 registry drift), and characterizes the two new `c-discover`/`c-geo-grid` features as PARTIAL realizations of P1/P4. All previously-tracked items were cross-checked and NOT re-flagged as fresh.
